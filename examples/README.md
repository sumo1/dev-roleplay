# Examples

填充过的角色样例，脱敏后的参照。

纯骨架不够直观——这里放不同技术栈下的填充片段，供对照。

## 清单

| 示例 | 技术栈 | 场景 |
|------|--------|------|
| [typescript-api](./typescript-api/) | TypeScript + Node.js + 某 HTTP 框架 + 关系型数据库 | 给 Todo 服务加"按标签过滤"接口 |

每个示例只展示**角色输入 / 输出 / 文档填充形态**，不包含可运行的业务代码——示例的价值在于让读者看到"这套骨架填进真实项目后长什么样"。
